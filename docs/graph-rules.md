# Graph Rules

- No cycles
- One main child per node
- Maximum two side branches per node
- Deterministic override when LLM output violates rules
- Main branch summary uses the latest five main-branch nodes, truncated to 600 characters

